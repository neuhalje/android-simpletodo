<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="name.neuhalfen.todosimple.todosimple" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="name.neuhalfen.todosimple.todosimple.TodoListActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="name.neuhalfen.todosimple.todosimple.TodoDetailActivity"
            android:label="@string/title_todo_detail"
            android:parentActivityName="name.neuhalfen.todosimple.todosimple.TodoListActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="name.neuhalfen.todosimple.todosimple.TodoListActivity" />
        </activity>
    </application>

</manifest>
